# Авторизация, часть 1 из 4 — БД: миграции и стор

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать стору механизм миграций схемы, завести таблицы `identities`/`sessions`/`email_tokens`, перевести состояние с имени аккаунта на `user_id`, добавить CRUD-методы под новые таблицы — всё под тестами, с безопасной раскаткой на прод.

**Architecture:** Дуальный стор (Postgres прод / SQLite dev+тесты) получает раннер нумерованных `.sql`-миграций через `go:embed`, с опциональным Go-хуком на версию (для шагов, которым нужен генератор id и джойны). Идентичность моделируется таблицей `identities` (провайдеры `password`, `telegram`); 5 таблиц состояния переkey-ятся с `user_name` на `user_id` через пересбор таблиц (портируемо PG/SQLite). Существующие аккаунты `Гриша`/`Алина` привязываются к TG-identity, тестовый мусор удаляется.

**Tech Stack:** Go 1.25 (stdlib `database/sql`, `embed`, `crypto/rand`, `crypto/sha256`), `modernc.org/sqlite`, `jackc/pgx/v5`. Тесты — `go test ./server/internal/store/`, SQLite in-memory + `make test-pg`.

**Spec:** [docs/superpowers/specs/2026-09-09-auth-postgres-profile-design.md](../specs/2026-09-09-auth-postgres-profile-design.md) — секция 1.

**Последующие части:** 2 — пакеты `auth`/`mail`/`ratelimit` + эндпойнты `/api/auth/*`, `/api/me*` + middleware; 3 — фронт (вход, профиль, навбар); 4 — SMTP, Telegram, бэкапы.

## Global Constraints

- Модуль Go: `github.com/grisha/serbian-app`. Все импорты внутренних пакетов — от этого префикса.
- Go 1.25.0 (`go.mod`). Не поднимать.
- Дуальный SQL: запросы пишутся с `?`-плейсхолдерами, `store.rebind` (в `store.go`) переписывает в `$N` для Postgres. Отличие диалектов в схеме — только макрос автоинкремента (`{{.AutoID}}`); всё остальное переносимо. Строковые литералы в миграциях не содержат `;`.
- ID генерятся в Go (пакет `internal/auth`, Task 1): `usr_` + 22 символа base62 для `users.id`, `idn_` для `identities.id`. Никаких `gen_random_uuid()` / `uuid`-расширений в SQL.
- Время в БД — строки RFC3339 (`time.Now().UTC().Format(time.RFC3339)`), как уже принято в проекте.
- Хеш токенов в БД: `hex(sha256(raw))` (эти таблицы заполняет часть 2; здесь только колонки `token_hash`).
- FK `ON DELETE CASCADE` на `identities`/`sessions`/`email_tokens` → `users`. Postgres форсит; для SQLite `Open` уже ставит `PRAGMA foreign_keys=ON` (Task 2). Таблицы состояния — без FK.
- Комментарии в коде — по-английски, короткие доки на экспортных символах, ошибки оборачивать `fmt.Errorf("...: %w", err)`.
- Тесты рядом с кодом, table-driven где уместно. Стор гоняется на SQLite in-memory; для Postgres — `make test-pg` (`TEST_DATABASE_URL`).
- Коммиты подписывать `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`.
- Не трогать: контент в `content/`, пакеты `internal/srs`, `internal/checker`, `internal/content`, фронт `web/`.

---

## Карта файлов

### Создаются

| Файл | Ответственность |
|---|---|
| `server/internal/auth/id.go` (+`id_test.go`) | Генерация id (`usr_…`, `idn_…`). Начало пакета `auth`. |
| `server/internal/store/migrate.go` (+`migrate_test.go`) | Раннер миграций: `schema_migrations`, `go:embed`, макрос `{{.AutoID}}`, Go-хуки на версию. |
| `server/internal/store/migrations/001_init.sql` | Текущая схема, свёрнутая. |
| `server/internal/store/migrations/002_auth.sql` | `*_new`-таблицы под ре-кей (DDL). |
| _(нет файла `003`)_ | Версия 3 — только Go-хук `migrate003`; `loadMigrations` синтезирует её из `hooks`. |
| `server/internal/store/migration_hooks.go` | Go-хуки `migrate002`, `migrate003`. |
| `server/internal/store/identities.go` (+`identities_test.go`) | CRUD `identities`. |
| `server/internal/store/sessions.go` (+`sessions_test.go`) | CRUD `sessions`. |
| `server/internal/store/email_tokens.go` (+`email_tokens_test.go`) | CRUD `email_tokens`. |

### Модифицируются

| Файл | Что меняется |
|---|---|
| `server/internal/store/store.go` | `applySchema` → v1→v2-ветка + `runMigrations()`; `SetMaxOpenConns(10→5)`; `PRAGMA foreign_keys=ON`; `UserStore.user` = `user_id`; `User(id string)`; account-методы (`CreateUser`, `UserByID`, `ListUsers` c id+name); `AllUsersProgress` джойнит `name`. |
| `server/internal/store/store_test.go` | `newUser` создаёт юзера и берёт его id; TRUNCATE-список PG + новые таблицы. |
| `server/internal/store/import.go` | `ImportSQLite` генерит `users.id`, пишет состояние по `user_id`. |

### Удаляются

Ничего.

---

## Task 1: Генерация ID (пакет `auth`)

Только stdlib (`crypto/rand`, `math/big`). Внешние зависимости `x/crypto`
(bcrypt) и `x/time` (rate) добавляются в **части 2**, когда их реально
импортируют — `go mod tidy` вычистил бы их сейчас как неиспользуемые.

**Files:**
- Create: `server/internal/auth/id.go`
- Test: `server/internal/auth/id_test.go`

**Interfaces:**
- Consumes: ничего.
- Produces:
  - `auth.NewID(prefix string) string` — `<prefix>_` + 22 символа base62 из `crypto/rand`.
  - `auth.NewUserID() string` → `NewID("usr")`
  - `auth.NewIdentityID() string` → `NewID("idn")`

- [ ] **Step 1: Написать падающий тест**

Create `server/internal/auth/id_test.go`:
```go
package auth

import (
	"regexp"
	"testing"
)

func TestNewID(t *testing.T) {
	re := regexp.MustCompile(`^usr_[0-9A-Za-z]{22}$`)
	seen := map[string]bool{}
	for i := 0; i < 1000; i++ {
		id := NewUserID()
		if !re.MatchString(id) {
			t.Fatalf("bad id: %q", id)
		}
		if seen[id] {
			t.Fatalf("collision: %q", id)
		}
		seen[id] = true
	}
	if got := NewIdentityID()[:4]; got != "idn_" {
		t.Fatalf("prefix = %q", got)
	}
}
```

- [ ] **Step 2: Прогнать — падает**

Run: `go test ./server/internal/auth/ -run TestNewID -v`
Expected: FAIL (`undefined: NewUserID`).

- [ ] **Step 3: Реализация**

Create `server/internal/auth/id.go`:
```go
// Package auth holds authentication primitives: id generation, password
// hashing, opaque token handling, and Telegram signature validation.
package auth

import (
	"crypto/rand"
	"math/big"
)

const base62 = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz"

// NewID returns "<prefix>_" followed by 22 base62 characters drawn from a
// cryptographic source (~131 bits of entropy).
func NewID(prefix string) string {
	b := make([]byte, 22)
	max := big.NewInt(int64(len(base62)))
	for i := range b {
		n, err := rand.Int(rand.Reader, max)
		if err != nil {
			panic(err) // crypto/rand failure is unrecoverable
		}
		b[i] = base62[n.Int64()]
	}
	return prefix + "_" + string(b)
}

// NewUserID mints an id for a users row.
func NewUserID() string { return NewID("usr") }

// NewIdentityID mints an id for an identities row.
func NewIdentityID() string { return NewID("idn") }
```

- [ ] **Step 4: Прогнать — проходит**

Run: `go test ./server/internal/auth/ -run TestNewID -v`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add server/internal/auth/
git commit -m "$(printf 'feat(auth): id generation for users and identities\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

## Task 2: Раннер миграций + `001_init.sql`

**Files:**
- Create: `server/internal/store/migrate.go`, `server/internal/store/migrations/001_init.sql`
- Modify: `server/internal/store/store.go` (функция `Open`, `applySchema`→`runMigrations`; `schemaSQL` остаётся только для `migrateV1toV2`)
- Test: `server/internal/store/migrate_test.go`

**Interfaces:**
- Consumes: ничего.
- Produces:
  - `store.Store.runMigrations() error` — приватный, зовётся из `Open`.
  - Таблица `schema_migrations(version INTEGER PRIMARY KEY, applied_at TEXT NOT NULL)`.
  - Файлы `migrations/NNN_*.sql`, применяются по возрастанию `NNN`, каждый в своей транзакции.

- [ ] **Step 1: Падающий тест**

Create `server/internal/store/migrate_test.go`:
```go
package store

import "testing"

func TestMigrationsIdempotent(t *testing.T) {
	s := newStore(t) // newStore already calls Open -> runMigrations
	// second Open on the same in-memory db is a fresh db, so instead
	// re-run the runner directly and assert no error / no dup rows.
	if err := s.runMigrations(); err != nil {
		t.Fatalf("re-run: %v", err)
	}
	var n int
	if err := s.db.QueryRow(`SELECT COUNT(*) FROM schema_migrations`).Scan(&n); err != nil {
		t.Fatal(err)
	}
	if n < 1 {
		t.Fatalf("schema_migrations empty")
	}
	// core tables from 001 exist
	for _, tbl := range []string{"users", "srs_cards", "reviews", "attempts", "lesson_progress", "lesson_step_progress"} {
		if _, err := s.db.Query(`SELECT 1 FROM ` + tbl + ` LIMIT 1`); err != nil {
			t.Fatalf("table %s missing: %v", tbl, err)
		}
	}
}

func TestMigrationsRecordVersions(t *testing.T) {
	s := newStore(t)
	rows, err := s.db.Query(`SELECT version FROM schema_migrations ORDER BY version`)
	if err != nil {
		t.Fatal(err)
	}
	defer rows.Close()
	var got []int
	for rows.Next() {
		var v int
		_ = rows.Scan(&v)
		got = append(got, v)
	}
	if len(got) == 0 || got[0] != 1 {
		t.Fatalf("versions = %v, want first = 1", got)
	}
}
```

- [ ] **Step 2: Прогнать — падает**

Run: `go test ./server/internal/store/ -run TestMigrations -v`
Expected: FAIL (`s.runMigrations undefined`).

- [ ] **Step 3: `001_init.sql`**

Create `server/internal/store/migrations/001_init.sql` — перенести туда содержимое текущего `schemaSQL(false)` дословно (SQLite-вариант автоинкремента), заменив `INTEGER PRIMARY KEY AUTOINCREMENT` на макрос `{{.AutoID}}`:
```sql
CREATE TABLE IF NOT EXISTS users (
	name       TEXT PRIMARY KEY,
	created_at TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS srs_cards (
	user_name     TEXT NOT NULL,
	card_id       TEXT NOT NULL,
	kind          TEXT NOT NULL,
	ref_id        TEXT NOT NULL,
	ease          REAL NOT NULL DEFAULT 2.5,
	interval_days INTEGER NOT NULL DEFAULT 0,
	reps          INTEGER NOT NULL DEFAULT 0,
	lapses        INTEGER NOT NULL DEFAULT 0,
	state         TEXT NOT NULL DEFAULT 'new',
	due           TEXT,
	updated_at    TEXT NOT NULL,
	PRIMARY KEY (user_name, card_id)
);
CREATE TABLE IF NOT EXISTS reviews (
	id          {{.AutoID}},
	user_name   TEXT NOT NULL,
	card_id     TEXT NOT NULL,
	grade       INTEGER NOT NULL,
	reviewed_at TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS attempts (
	id           {{.AutoID}},
	user_name    TEXT NOT NULL,
	exercise_id  TEXT NOT NULL,
	lesson       TEXT NOT NULL,
	block        TEXT NOT NULL,
	answer       TEXT NOT NULL,
	correct      INTEGER NOT NULL,
	attempted_at TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS lesson_progress (
	user_name    TEXT NOT NULL,
	lesson       TEXT NOT NULL,
	status       TEXT NOT NULL,
	started_at   TEXT,
	completed_at TEXT,
	PRIMARY KEY (user_name, lesson)
);
CREATE TABLE IF NOT EXISTS lesson_step_progress (
	user_name    TEXT NOT NULL,
	lesson       TEXT NOT NULL,
	step         TEXT NOT NULL,
	status       TEXT NOT NULL,
	completed_at TEXT,
	PRIMARY KEY (user_name, lesson, step)
);
CREATE INDEX IF NOT EXISTS reviews_user ON reviews(user_name);
CREATE INDEX IF NOT EXISTS attempts_user ON attempts(user_name);
```

- [ ] **Step 4: `migrate.go`**

Create `server/internal/store/migrate.go`:
```go
package store

import (
	"embed"
	"fmt"
	"sort"
	"strconv"
	"strings"
	"time"
)

//go:embed migrations/*.sql
var migrationFS embed.FS

// autoIDExpr is the dialect-specific autoincrement PK column definition,
// substituted for the {{.AutoID}} macro in migration files.
func autoIDExpr(pg bool) string {
	if pg {
		return "BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY"
	}
	return "INTEGER PRIMARY KEY AUTOINCREMENT"
}

type migration struct {
	version int
	name    string
	sql     string
}

// loadMigrations reads and orders migrations/NNN_*.sql from the embed FS.
func loadMigrations() ([]migration, error) {
	entries, err := migrationFS.ReadDir("migrations")
	if err != nil {
		return nil, err
	}
	var out []migration
	for _, e := range entries {
		if e.IsDir() || !strings.HasSuffix(e.Name(), ".sql") {
			continue
		}
		num, _, ok := strings.Cut(e.Name(), "_")
		if !ok {
			return nil, fmt.Errorf("migration %q: expected NNN_name.sql", e.Name())
		}
		v, err := strconv.Atoi(num)
		if err != nil {
			return nil, fmt.Errorf("migration %q: bad version: %w", e.Name(), err)
		}
		b, err := migrationFS.ReadFile("migrations/" + e.Name())
		if err != nil {
			return nil, err
		}
		out = append(out, migration{version: v, name: e.Name(), sql: string(b)})
	}
	sort.Slice(out, func(i, j int) bool { return out[i].version < out[j].version })
	return out, nil
}

// runMigrations applies every migration whose version is not yet recorded
// in schema_migrations, each in its own transaction, lowest version first.
func (s *Store) runMigrations() error {
	if _, err := s.db.Exec(`CREATE TABLE IF NOT EXISTS schema_migrations (
		version INTEGER PRIMARY KEY, applied_at TEXT NOT NULL)`); err != nil {
		return fmt.Errorf("schema_migrations: %w", err)
	}
	applied := map[int]bool{}
	rows, err := s.db.Query(`SELECT version FROM schema_migrations`)
	if err != nil {
		return err
	}
	for rows.Next() {
		var v int
		if err := rows.Scan(&v); err != nil {
			rows.Close()
			return err
		}
		applied[v] = true
	}
	rows.Close()

	migs, err := loadMigrations()
	if err != nil {
		return err
	}
	macro := autoIDExpr(s.db.pg)
	for _, m := range migs {
		if applied[m.version] {
			continue
		}
		script := strings.ReplaceAll(m.sql, "{{.AutoID}}", macro)
		tx, err := s.db.Begin()
		if err != nil {
			return err
		}
		for _, stmt := range splitSQL(script) {
			if _, err := tx.Exec(stmt); err != nil {
				tx.Rollback()
				return fmt.Errorf("migration %s: %s: %w", m.name, firstLine(stmt), err)
			}
		}
		if _, err := tx.Exec(`INSERT INTO schema_migrations (version, applied_at) VALUES (?, ?)`,
			m.version, time.Now().UTC().Format(time.RFC3339)); err != nil {
			tx.Rollback()
			return fmt.Errorf("record %s: %w", m.name, err)
		}
		if err := tx.Commit(); err != nil {
			return fmt.Errorf("commit %s: %w", m.name, err)
		}
	}
	return nil
}

// splitSQL breaks a multi-statement script on ';' at statement level.
// Migration files must not contain ';' inside string literals.
func splitSQL(script string) []string {
	var out []string
	for _, s := range strings.Split(script, ";") {
		if strings.TrimSpace(s) != "" {
			out = append(out, s)
		}
	}
	return out
}
```

- [ ] **Step 5: Подключить в `Open`**

Modify `server/internal/store/store.go`:
- В `Open`, для SQLite добавить `PRAGMA foreign_keys=ON` в существующий `Exec` со списком PRAGMA:
  ```go
  if _, err := sqlDB.Exec(`PRAGMA journal_mode=WAL; PRAGMA busy_timeout=5000; PRAGMA foreign_keys=ON;`); err != nil {
  ```
- Для Postgres поменять `sqlDB.SetMaxOpenConns(10)` → `sqlDB.SetMaxOpenConns(5)`.
- Заменить тело `applySchema` — оставить только v1→v2-ветку (SQLite), а после неё вызвать раннер:
  ```go
  func (s *Store) applySchema() error {
  	if !s.db.pg {
  		var ver int
  		_ = s.db.QueryRow(`PRAGMA user_version`).Scan(&ver)
  		if ver == 1 {
  			if err := s.migrateV1toV2(); err != nil {
  				return fmt.Errorf("migrate v1->v2: %w", err)
  			}
  			if _, err := s.db.Exec(`PRAGMA user_version = 2`); err != nil {
  				return err
  			}
  		}
  	}
  	return s.runMigrations()
  }
  ```
- `schemaVer` const и `schemaSQL(pg)` — оставить (их использует `migrateV1toV2`); в `migrateV1toV2` заменить `schemaSQL(false)` вызов ок как есть.

> Примечание: `migrateV1toV2` создаёт v2-таблицы через `schemaSQL(false)`; затем `runMigrations` увидит их существующими (`IF NOT EXISTS`) и просто запишет `version=1`. Совместимо.

- [ ] **Step 6: Прогнать — проходит**

Run: `go test ./server/internal/store/ -run 'TestMigrations|TestEnsure|TestGrade' -v`
Expected: PASS (миграции + подвыборка существующих тестов стора).

- [ ] **Step 7: Полный прогон стора**

Run: `go test ./server/internal/store/ -v`
Expected: PASS (все существующие тесты — схема не изменилась семантически).

- [ ] **Step 8: Коммит**

```bash
git add server/internal/store/
git commit -m "$(printf 'feat(store): numbered migration runner, fold schema into 001\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

## Task 3: Схема авторизации — миграция `002`

Добавляет `identities` / `sessions` / `email_tokens`, колонку `users.id` (PK),
и переводит 5 таблиц состояния с `user_name` на `user_id`. Генерация id
существующим строкам — Go-хук в той же транзакции миграции (чистый SQL это
портируемо не сделает).

**Files:**
- Modify: `server/internal/store/migrate.go` (Go-хуки на версию)
- Create: `server/internal/store/migrations/002_auth.sql`
- Modify: `server/internal/store/store_test.go` (TRUNCATE-список PG)
- Test: `server/internal/store/migrate_test.go` (дополнить)

**Interfaces:**
- Consumes: `auth.NewUserID` (Task 1), `runMigrations` (Task 2).
- Produces:
  - Таблицы `identities`, `sessions`, `email_tokens` (колонки — см. спеку §1.2).
  - `users(id TEXT PK, name TEXT NOT NULL, created_at TEXT NOT NULL)`.
  - Состояние: `srs_cards/reviews/attempts/lesson_progress/lesson_step_progress` с колонкой `user_id` вместо `user_name`, те же составные PK и индексы.
  - Механизм: `store.registerHook(version int, fn func(tx *dbtx, pg bool) error)`.

- [ ] **Step 1: Падающий тест**

Дополнить `server/internal/store/migrate_test.go`:
```go
func TestMigration002Schema(t *testing.T) {
	s := newStore(t)
	// new tables
	for _, tbl := range []string{"identities", "sessions", "email_tokens"} {
		if _, err := s.db.Query(`SELECT 1 FROM ` + tbl + ` LIMIT 1`); err != nil {
			t.Fatalf("table %s missing: %v", tbl, err)
		}
	}
	// users has id, state tables have user_id
	if _, err := s.db.Query(`SELECT id, name, created_at FROM users LIMIT 1`); err != nil {
		t.Fatalf("users.id missing: %v", err)
	}
	for _, tbl := range []string{"srs_cards", "reviews", "attempts", "lesson_progress", "lesson_step_progress"} {
		if _, err := s.db.Query(`SELECT user_id FROM ` + tbl + ` LIMIT 1`); err != nil {
			t.Fatalf("%s.user_id missing: %v", tbl, err)
		}
	}
}

func TestMigration002RekeysExistingData(t *testing.T) {
	// Build a pre-002 db by hand: run only 001, seed name-keyed rows, then
	// run the rest. mustOpenRaw / runMigrationsUpTo are added in Step 3b.
	s := &Store{db: mustOpenRaw(t)}
	if err := s.runMigrationsUpTo(1); err != nil { t.Fatal(err) }
	mustExec(t, s, `INSERT INTO users (name, created_at) VALUES ('Гриша', '2026-09-06T10:00:00Z')`)
	mustExec(t, s, `INSERT INTO lesson_progress (user_name, lesson, status) VALUES ('Гриша', '01', 'done')`)
	if err := s.runMigrations(); err != nil { t.Fatal(err) }

	var uid string
	if err := s.db.QueryRow(`SELECT id FROM users WHERE name='Гриша'`).Scan(&uid); err != nil {
		t.Fatal(err)
	}
	if uid == "" || uid[:4] != "usr_" { t.Fatalf("bad user id %q", uid) }
	var got string
	if err := s.db.QueryRow(`SELECT user_id FROM lesson_progress WHERE lesson='01'`).Scan(&got); err != nil {
		t.Fatal(err)
	}
	if got != uid { t.Fatalf("progress user_id = %q, want %q", got, uid) }
}
```
Добавить в `migrate_test.go` мелкие хелперы `mustOpenRaw`, `applyMigrationsUpTo(maxVersion int)`, `mustExec` (5–10 строк каждый; `applyMigrationsUpTo` — копия цикла `runMigrations` с условием `m.version <= maxVersion` и без хуков выше лимита).

- [ ] **Step 2: Прогнать — падает**

Run: `go test ./server/internal/store/ -run TestMigration002 -v`
Expected: FAIL (нет таблиц / нет `user_id`).

- [ ] **Step 3: Go-хуки в раннере**

Modify `server/internal/store/migrate.go`:
```go
// hooks holds Go steps that run inside a migration's transaction, keyed by
// version, after that version's .sql (if any). Registered from migration_hooks.go.
var hooks = map[int]func(tx *dbtx, pg bool) error{}

func registerHook(version int, fn func(tx *dbtx, pg bool) error) { hooks[version] = fn }
```
`registerHook` will be called from `migration_hooks.go` (Step 5, `init()`).
In the `runMigrations` loop, after applying the migration's statements and
**before** `INSERT INTO schema_migrations`, run the version's hook inside the
same transaction:
```go
if hook := hooks[m.version]; hook != nil {
	if err := hook(tx, s.db.pg); err != nil {
		tx.Rollback()
		return fmt.Errorf("migration %s hook: %w", m.name, err)
	}
}
```
Do **not** touch `loadMigrations` in this task — every version so far has a
`.sql` file (`001`, `002`). Synthesizing hook-only versions is Task 4's job
(version 3 is the first hook-only migration).

- [ ] **Step 3b: Тест-сим для «до 002»**

В `migrate.go` добавить неэкспортируемый seam для тестов:
```go
// runMigrationsUpTo applies migrations with version <= max (test helper).
func (s *Store) runMigrationsUpTo(max int) error { return s.runMigrationsFiltered(max) }
```
Вынести тело `runMigrations` в `runMigrationsFiltered(max int)` (при `max <= 0`
— без лимита), а `runMigrations()` = `runMigrationsFiltered(0)`. В цикле —
`if max > 0 && m.version > max { continue }`. Хелперы теста
(`mustOpenRaw`, `mustExec`) — в `migrate_test_helper_test.go`:
```go
func mustOpenRaw(t *testing.T) *database {
	t.Helper()
	db, err := sql.Open("sqlite", ":memory:")
	if err != nil { t.Fatal(err) }
	db.SetMaxOpenConns(1)
	db.Exec(`PRAGMA foreign_keys=ON`)
	t.Cleanup(func() { db.Close() })
	return &database{sqlDB: db, pg: false}
}
func mustExec(t *testing.T, s *Store, q string, a ...any) {
	t.Helper()
	if _, err := s.db.Exec(q, a...); err != nil { t.Fatalf("%s: %v", q, err) }
}
```
- [ ] **Step 4: `002_auth.sql` — только `*_new`-таблицы**

`identities`/`sessions`/`email_tokens` создаёт хук (Step 5) **после** свопа
`users`, иначе на SQLite `DROP TABLE users` упрётся в их FK. Файл
`server/internal/store/migrations/002_auth.sql`:
```sql
CREATE TABLE users_new (
	id         TEXT PRIMARY KEY,
	name       TEXT NOT NULL,
	created_at TEXT NOT NULL
);

CREATE TABLE srs_cards_new (
	user_id       TEXT NOT NULL,
	card_id       TEXT NOT NULL,
	kind          TEXT NOT NULL,
	ref_id        TEXT NOT NULL,
	ease          REAL NOT NULL DEFAULT 2.5,
	interval_days INTEGER NOT NULL DEFAULT 0,
	reps          INTEGER NOT NULL DEFAULT 0,
	lapses        INTEGER NOT NULL DEFAULT 0,
	state         TEXT NOT NULL DEFAULT 'new',
	due           TEXT,
	updated_at    TEXT NOT NULL,
	PRIMARY KEY (user_id, card_id)
);
CREATE TABLE reviews_new (
	id          {{.AutoID}},
	user_id     TEXT NOT NULL,
	card_id     TEXT NOT NULL,
	grade       INTEGER NOT NULL,
	reviewed_at TEXT NOT NULL
);
CREATE TABLE attempts_new (
	id           {{.AutoID}},
	user_id      TEXT NOT NULL,
	exercise_id  TEXT NOT NULL,
	lesson       TEXT NOT NULL,
	block        TEXT NOT NULL,
	answer       TEXT NOT NULL,
	correct      INTEGER NOT NULL,
	attempted_at TEXT NOT NULL
);
CREATE TABLE lesson_progress_new (
	user_id      TEXT NOT NULL,
	lesson       TEXT NOT NULL,
	status       TEXT NOT NULL,
	started_at   TEXT,
	completed_at TEXT,
	PRIMARY KEY (user_id, lesson)
);
CREATE TABLE lesson_step_progress_new (
	user_id      TEXT NOT NULL,
	lesson       TEXT NOT NULL,
	step         TEXT NOT NULL,
	status       TEXT NOT NULL,
	completed_at TEXT,
	PRIMARY KEY (user_id, lesson, step)
);
```

- [ ] **Step 5: Хук `002` — заполнить id и переложить данные**

Create `server/internal/store/migration_hooks.go`:
```go
package store

import (
	"time"

	"github.com/grisha/serbian-app/server/internal/auth"
)

func init() {
	registerHook(2, migrate002)
}

// migrate002 fills users_new with a generated id per legacy row, re-keys the
// five state tables from user_name to user_id, then swaps *_new into place.
// Runs inside migration 002's transaction, after 002_auth.sql.
func migrate002(tx *dbtx, pg bool) error {
	rows, err := tx.Query(`SELECT name, created_at FROM users ORDER BY created_at`)
	if err != nil {
		return err
	}
	type u struct{ name, created string }
	var legacy []u
	for rows.Next() {
		var x u
		if err := rows.Scan(&x.name, &x.created); err != nil {
			rows.Close()
			return err
		}
		legacy = append(legacy, x)
	}
	rows.Close()

	idByName := map[string]string{}
	for _, x := range legacy {
		id := auth.NewUserID()
		idByName[x.name] = id
		if _, err := tx.Exec(`INSERT INTO users_new (id, name, created_at) VALUES (?, ?, ?)`,
			id, x.name, x.created); err != nil {
			return err
		}
	}

	// state tables: copy rows whose user_name resolves to a known id
	copies := []struct{ dst, src, cols string }{
		{"srs_cards_new", "srs_cards", "card_id, kind, ref_id, ease, interval_days, reps, lapses, state, due, updated_at"},
		{"reviews_new", "reviews", "card_id, grade, reviewed_at"},
		{"attempts_new", "attempts", "exercise_id, lesson, block, answer, correct, attempted_at"},
		{"lesson_progress_new", "lesson_progress", "lesson, status, started_at, completed_at"},
		{"lesson_step_progress_new", "lesson_step_progress", "lesson, step, status, completed_at"},
	}
	for _, c := range copies {
		src, err := tx.Query(`SELECT user_name, ` + c.cols + ` FROM ` + c.src)
		if err != nil {
			return err
		}
		var batch [][]any
		ncol := len(splitCols(c.cols))
		for src.Next() {
			vals := make([]any, ncol+1)
			ptrs := make([]any, ncol+1)
			for i := range vals {
				ptrs[i] = &vals[i]
			}
			if err := src.Scan(ptrs...); err != nil {
				src.Close()
				return err
			}
			batch = append(batch, vals)
		}
		src.Close()
		ph := "?" + strings0(", ?", ncol) // ncol+1 placeholders: user_id + ncol copied cols
		ins := `INSERT INTO ` + c.dst + ` (user_id, ` + c.cols + `) VALUES (` + ph + `)`
		for _, vals := range batch {
			name, _ := vals[0].(string)
			uid, ok := idByName[name]
			if !ok {
				continue // orphaned row (deleted account) — dropped
			}
			args := append([]any{uid}, vals[1:]...)
			if _, err := tx.Exec(ins, args...); err != nil {
				return err
			}
		}
	}

	// swap state tables + users
	for _, name := range []string{"users", "srs_cards", "reviews", "attempts", "lesson_progress", "lesson_step_progress"} {
		if _, err := tx.Exec(`DROP TABLE ` + name); err != nil {
			return err
		}
		if _, err := tx.Exec(`ALTER TABLE ` + name + `_new RENAME TO ` + name); err != nil {
			return err
		}
	}
	for _, q := range []string{
		`CREATE INDEX reviews_user ON reviews(user_id)`,
		`CREATE INDEX attempts_user ON attempts(user_id)`,
	} {
		if _, err := tx.Exec(q); err != nil {
			return err
		}
	}

	// auth tables — created here, after users is in place, so FKs resolve
	for _, q := range auth002Tables {
		if _, err := tx.Exec(q); err != nil {
			return err
		}
	}
	return nil
}

// auth002Tables is the DDL for identities/sessions/email_tokens + indexes,
// portable across SQLite and Postgres (no {{.AutoID}} needed — all TEXT PKs).
var auth002Tables = []string{
	`CREATE TABLE identities (
		id TEXT PRIMARY KEY,
		user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
		provider TEXT NOT NULL, provider_uid TEXT NOT NULL,
		email TEXT, password_hash TEXT, email_verified_at TEXT, tg_username TEXT,
		created_at TEXT NOT NULL,
		UNIQUE (provider, provider_uid))`,
	`CREATE TABLE sessions (
		token_hash TEXT PRIMARY KEY,
		user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
		created_at TEXT NOT NULL, last_seen_at TEXT NOT NULL,
		expires_at TEXT NOT NULL, user_agent TEXT)`,
	`CREATE TABLE email_tokens (
		token_hash TEXT PRIMARY KEY,
		identity_id TEXT NOT NULL REFERENCES identities(id) ON DELETE CASCADE,
		kind TEXT NOT NULL, created_at TEXT NOT NULL,
		expires_at TEXT NOT NULL, used_at TEXT)`,
	`CREATE INDEX identities_user ON identities(user_id)`,
	`CREATE INDEX sessions_user ON sessions(user_id)`,
	`CREATE INDEX sessions_expires ON sessions(expires_at)`,
	`CREATE INDEX email_tokens_ident ON email_tokens(identity_id)`,
}
```
Плюс хелперы в `migration_hooks.go`: `splitCols(s string) []string`
(`strings.Split(s, ", ")`) и `strings0(sep string, n int) string`
(`strings.Repeat(sep, n)`). Импорты файла: `strings`, `time`,
`github.com/grisha/serbian-app/server/internal/auth`.

- [ ] **Step 6: TRUNCATE-список для PG-тестов**

Modify `server/internal/store/store_test.go` — в `newStore`, строка `TRUNCATE users, srs_cards, ...`:
```go
s.db.Exec(`TRUNCATE users, identities, sessions, email_tokens, srs_cards, reviews, attempts, lesson_progress, lesson_step_progress`)
```

- [ ] **Step 7: Прогнать**

Run: `go test ./server/internal/store/ -run TestMigration -v`
Expected: PASS.

- [ ] **Step 8: Полный прогон стора (ожидаемо часть падает)**

Run: `go test ./server/internal/store/ -v`
Expected: FAIL в тестах, которые дергают `s.User("Гриша")` / `EnsureUser` — это чинится в Task 5. Зафиксировать список падающих в сообщении коммита.

- [ ] **Step 9: Коммит**

```bash
git add server/internal/store/
git commit -m "$(printf 'feat(store): migration 002 — identities/sessions/tokens, re-key state to user_id\n\nStore tests that key by name still fail; fixed in the store re-key task.\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

## Task 4: Миграция аккаунтов — хук `003`

Удаляет тестовые аккаунты и их данные, заводит `Гриша`/`Алина` как
`telegram`-identity с `provider_uid = 'pending:<username>'` (реальный числовой
`tg_id` подставится на первом TG-входе — часть 2, либо вручную).

**Files:**
- Modify: `server/internal/store/migrate.go` (`loadMigrations` — версии только из хуков)
- Modify: `server/internal/store/migration_hooks.go` (+`migrate003`)
- Test: `server/internal/store/migrate_test.go` (+кейс)

**Interfaces:**
- Consumes: `auth.NewIdentityID` (Task 1), `registerHook` (Task 3).
- Produces: после миграций в чистой БД аккаунтов нет; в БД с legacy-строками
  `DeployCheck/ProbaPG/chk2/kbcheck` они и их состояние удалены, а у
  `Гриша`/`Алина` есть строка `identities` c `provider='telegram'`,
  `provider_uid='pending:llladnooo'` / `'pending:alinsssk'`.

- [ ] **Step 1: Тест**

```go
func TestMigration003Accounts(t *testing.T) {
	s := &Store{db: mustOpenRaw(t)}
	if err := s.applyMigrationsUpTo(1); err != nil { t.Fatal(err) }
	for _, n := range []string{"Гриша", "Алина", "DeployCheck", "chk2"} {
		mustExec(t, s, `INSERT INTO users (name, created_at) VALUES (?, '2026-09-06T10:00:00Z')`, n)
	}
	mustExec(t, s, `INSERT INTO lesson_progress (user_name, lesson, status) VALUES ('chk2', '01', 'done')`)
	if err := s.runMigrations(); err != nil { t.Fatal(err) }

	var n int
	s.db.QueryRow(`SELECT COUNT(*) FROM users WHERE name IN ('DeployCheck','chk2')`).Scan(&n)
	if n != 0 { t.Fatalf("junk users left: %d", n) }
	s.db.QueryRow(`SELECT COUNT(*) FROM lesson_progress`).Scan(&n)
	if n != 0 { t.Fatalf("junk progress left: %d", n) }
	var uid, puid string
	if err := s.db.QueryRow(`SELECT i.user_id, i.provider_uid FROM identities i
		JOIN users u ON u.id=i.user_id WHERE u.name='Гриша' AND i.provider='telegram'`).Scan(&uid, &puid); err != nil {
		t.Fatalf("Гриша telegram identity: %v", err)
	}
	if puid != "pending:llladnooo" { t.Fatalf("provider_uid = %q", puid) }
}
```

- [ ] **Step 2: Прогнать — падает**

Run: `go test ./server/internal/store/ -run TestMigration003 -v`
Expected: FAIL.

- [ ] **Step 3: `loadMigrations` — версии из хуков**

В `migrate.go`, `loadMigrations`: после сбора файлов пройтись по ключам `hooks`
и добавить `migration{version: v, name: fmt.Sprintf("%03d_hook", v)}` для тех
версий, которых нет среди файлов. Пересортировать.

- [ ] **Step 4: `migrate003`**

В `migration_hooks.go`:
```go
func init() {
	registerHook(2, migrate002)
	registerHook(3, migrate003)
}

var junkAccounts = []string{"DeployCheck", "ProbaPG", "chk2", "kbcheck"}

var legacyTelegram = map[string]string{
	"Гриша": "llladnooo",
	"Алина": "alinsssk",
}

// migrate003 removes seed/test accounts and attaches the two real accounts to
// their Telegram identity (provider_uid "pending:<username>" until first login).
func migrate003(tx *dbtx, pg bool) error {
	stateTables := []string{"srs_cards", "reviews", "attempts", "lesson_progress", "lesson_step_progress"}
	for _, name := range junkAccounts {
		var id string
		err := tx.QueryRow(`SELECT id FROM users WHERE name = ?`, name).Scan(&id)
		if err != nil {
			continue // not present — fine
		}
		for _, tbl := range stateTables {
			if _, err := tx.Exec(`DELETE FROM `+tbl+` WHERE user_id = ?`, id); err != nil {
				return err
			}
		}
		if _, err := tx.Exec(`DELETE FROM identities WHERE user_id = ?`, id); err != nil {
			return err
		}
		if _, err := tx.Exec(`DELETE FROM users WHERE id = ?`, id); err != nil {
			return err
		}
	}
	for name, username := range legacyTelegram {
		var id string
		if err := tx.QueryRow(`SELECT id FROM users WHERE name = ?`, name).Scan(&id); err != nil {
			continue
		}
		var exists int
		_ = tx.QueryRow(`SELECT 1 FROM identities WHERE user_id = ? AND provider = 'telegram'`, id).Scan(&exists)
		if exists == 1 {
			continue
		}
		if _, err := tx.Exec(`INSERT INTO identities
			(id, user_id, provider, provider_uid, tg_username, created_at)
			VALUES (?, ?, 'telegram', ?, ?, ?)`,
			auth.NewIdentityID(), id, "pending:"+username, username,
			time.Now().UTC().Format(time.RFC3339)); err != nil {
			return err
		}
	}
	return nil
}
```
(добавить `import "github.com/grisha/serbian-app/server/internal/auth"` и `"time"` в файл — если ещё не.)

- [ ] **Step 5: Прогнать — проходит**

Run: `go test ./server/internal/store/ -run TestMigration -v`
Expected: PASS.

- [ ] **Step 6: Коммит**

```bash
git add server/internal/store/
git commit -m "$(printf 'feat(store): migration 003 — drop test accounts, seed Telegram identities\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

## Task 5: Ре-кей стора на `user_id`

Все per-user методы `UserStore` сейчас бьют `WHERE user_name = ?` с `u.user` =
имя. Переводим `u.user` на `user_id`, колонку — на `user_id`, `Store.User`
принимает id, добавляем account-методы. Чиним `store_test.go` и `import.go`.

**Files:**
- Modify: `server/internal/store/store.go`
- Modify: `server/internal/store/store_test.go`
- Modify: `server/internal/store/import.go`
- Test: существующие в `store_test.go` (должны снова зазеленеть) + новый кейс на account-методы.

**Interfaces:**
- Consumes: `auth.NewUserID` (Task 1).
- Produces:
  - `Store.CreateUser(name string) (id string, err error)` — вставляет `users(id, name, created_at)`, нормализует имя (`NormalizeName`, ≤ 40 рун), id из `auth.NewUserID()`.
  - `Store.User(id string) *UserStore` — `u.user = id`, без `NormalizeName`.
  - `Store.UserByID(id string) (UserRow, error)`, где `type UserRow struct { ID, Name, CreatedAt string }`; `sql.ErrNoRows` пробрасывается.
  - `Store.ListUsers() ([]UserRow, error)` — все, oldest-first (сигнатура меняется с `[]string`).
  - `Store.RenameUser(id, name string) error`.
  - `Store.DeleteUser(id string) error` — транзакция: `DELETE` из 5 таблиц состояния по `user_id`, затем `users` (identities/sessions/email_tokens уходят каскадом).
  - `UserStore` — все существующие методы без изменения сигнатур; внутри — `user_id`.
  - `AllUsersProgress` — сигнатура та же (`[]UserProgress`), `UserProgress.Name` теперь заполняется джойном.

- [ ] **Step 1: Обновить хелперы теста (падающий шаг)**

Modify `server/internal/store/store_test.go`:
```go
func newUser(t *testing.T) (*Store, *UserStore) {
	t.Helper()
	s := newStore(t)
	id, err := s.CreateUser("Гриша")
	if err != nil {
		t.Fatal(err)
	}
	return s, s.User(id)
}
```
Добавить кейс:
```go
func TestAccountLifecycle(t *testing.T) {
	s := newStore(t)
	id, err := s.CreateUser("  Гриша  ")
	if err != nil || id[:4] != "usr_" {
		t.Fatalf("create: %v %q", err, id)
	}
	row, err := s.UserByID(id)
	if err != nil || row.Name != "Гриша" {
		t.Fatalf("byID: %v %+v", err, row)
	}
	if err := s.RenameUser(id, "Гриша Н."); err != nil {
		t.Fatal(err)
	}
	u := s.User(id)
	if err := u.SetLessonStatus("01", "done", day0); err != nil {
		t.Fatal(err)
	}
	if err := s.DeleteUser(id); err != nil {
		t.Fatal(err)
	}
	if _, err := s.UserByID(id); err == nil {
		t.Fatalf("user still present")
	}
	var n int
	s.db.QueryRow(`SELECT COUNT(*) FROM lesson_progress WHERE user_id = ?`, id).Scan(&n)
	if n != 0 {
		t.Fatalf("state left after delete: %d", n)
	}
}
```
Также обновить любые тесты, зовущие `s.EnsureUser(...)` / `s.User("Имя")` /
`s.ListUsers()` (ожидали `[]string`) / `s.UserExists(...)` — переключить на
новые сигнатуры. Список таких мест получить `grep -n 'EnsureUser\|UserExists\|ListUsers\|\.User("' server/internal/store/*_test.go`.

- [ ] **Step 2: Прогнать — падает**

Run: `go test ./server/internal/store/ -run 'TestAccountLifecycle|TestEnsureCards' -v`
Expected: FAIL (`CreateUser` undefined и т.д.).

- [ ] **Step 3: `store.go` — account-методы**

- Удалить/заменить `EnsureUser`, `UserExists`; добавить `CreateUser`,
  `UserByID`, `RenameUser`, `DeleteUser`, тип `UserRow`.
- `ListUsers` → `[]UserRow` (`SELECT id, name, created_at FROM users ORDER BY created_at ASC`).
- `User(name string)` → `User(id string)`:
  ```go
  func (s *Store) User(id string) *UserStore { return &UserStore{db: s.db, user: id} }
  ```
- Полный код `CreateUser` / `DeleteUser`:
  ```go
  // CreateUser inserts a users row with a generated id and returns it.
  func (s *Store) CreateUser(name string) (string, error) {
  	n := NormalizeName(name)
  	if n == "" {
  		return "", fmt.Errorf("empty name")
  	}
  	if len([]rune(n)) > 40 {
  		return "", fmt.Errorf("name too long")
  	}
  	id := auth.NewUserID()
  	if _, err := s.db.Exec(`INSERT INTO users (id, name, created_at) VALUES (?, ?, ?)`,
  		id, n, time.Now().UTC().Format(time.RFC3339)); err != nil {
  		return "", err
  	}
  	return id, nil
  }

  // DeleteUser removes the account and all of its learning state in one tx.
  func (s *Store) DeleteUser(id string) error {
  	tx, err := s.db.Begin()
  	if err != nil {
  		return err
  	}
  	defer tx.Rollback()
  	for _, tbl := range []string{"srs_cards", "reviews", "attempts", "lesson_progress", "lesson_step_progress"} {
  		if _, err := tx.Exec(`DELETE FROM `+tbl+` WHERE user_id = ?`, id); err != nil {
  			return err
  		}
  	}
  	if _, err := tx.Exec(`DELETE FROM users WHERE id = ?`, id); err != nil {
  		return err
  	}
  	return tx.Commit()
  }
  ```
  (импортировать `internal/auth` в `store.go`.)

- [ ] **Step 4: `store.go` — per-user запросы**

В каждом методе `UserStore` заменить в SQL `user_name` → `user_id` (аргумент
`u.user` остаётся тем же по позиции). Затрагиваемые методы (все в `store.go`):
`EnsureCards`, `DueQueue`, `GradeCard`, `ActivateCard`, `ReviewedToday`,
`StreakDays`, `AddAttempt`, `WeakExercises`, `SetLessonStatus`,
`SetStepStatus`, `StepStatuses`, `LessonAttempts`, `ResetLesson`,
`ResetExercises`, `LessonStatuses`, `LessonStatus`, `ActivityByDay`,
`NewCount`, `DueCount`, `CardStats`. Проверить `grep -n user_name server/internal/store/store.go` — не должно остаться ничего, кроме `AllUsersProgress` (следующий шаг) и `migrateV1toV2`/`schemaSQL` (легаси, не трогать).

- [ ] **Step 5: `store.go` — `AllUsersProgress`**

Все агрегаты внутри — `user_name` → `user_id`, группировка по `user_id`;
результаты кладём в `map[string]…` по id; финальный проход — по `[]UserRow`.
Полный вид:
```go
func (s *Store) AllUsersProgress(today time.Time) ([]UserProgress, error) {
	users, err := s.ListUsers()
	if err != nil {
		return nil, err
	}
	lessons := map[string]int{}
	scanCount(s.db, `SELECT user_id, COUNT(*) FROM lesson_progress WHERE status='done' GROUP BY user_id`, lessons)

	known := map[string]int{}
	total := map[string]int{}
	if rows, err := s.db.Query(`SELECT user_id, COUNT(*),
		SUM(CASE WHEN state='review' AND interval_days>=7 THEN 1 ELSE 0 END)
		FROM srs_cards GROUP BY user_id`); err == nil {
		for rows.Next() {
			var id string
			var t, k int
			if rows.Scan(&id, &t, &k) == nil {
				total[id] = t
				known[id] = k
			}
		}
		rows.Close()
	}

	reviewedToday := map[string]int{}
	scanCount(s.db, `SELECT user_id, COUNT(*) FROM reviews WHERE substr(reviewed_at,1,10)='`+
		today.Format(dateFmt)+`' GROUP BY user_id`, reviewedToday)

	lastActive := map[string]string{}
	if rows, err := s.db.Query(`SELECT user_id, MAX(d) FROM (
		SELECT user_id, substr(reviewed_at,1,10) d FROM reviews
		UNION ALL SELECT user_id, substr(attempted_at,1,10) FROM attempts
	) GROUP BY user_id`); err == nil {
		for rows.Next() {
			var id, d string
			if rows.Scan(&id, &d) == nil {
				lastActive[id] = d
			}
		}
		rows.Close()
	}

	out := make([]UserProgress, 0, len(users))
	for _, ur := range users {
		streak, _ := s.User(ur.ID).StreakDays(today)
		out = append(out, UserProgress{
			Name: ur.Name, LessonsDone: lessons[ur.ID], CardsKnown: known[ur.ID],
			TotalCards: total[ur.ID], StreakDays: streak,
			ReviewedToday: reviewedToday[ur.ID], LastActive: lastActive[ur.ID],
		})
	}
	sort.Slice(out, func(i, j int) bool {
		if out[i].LessonsDone != out[j].LessonsDone {
			return out[i].LessonsDone > out[j].LessonsDone
		}
		return out[i].CardsKnown > out[j].CardsKnown
	})
	return out, nil
}
```
`scanCount`, `dateFmt`, `UserProgress` — без изменений.

- [ ] **Step 6: `import.go`**

`ImportSQLite`: таблица `users` теперь `(id, name, created_at)` — при чтении из
SQLite-источника (у него старая схема `name, created_at`) генерить `id` и
держать `map[name]id`; во вставках в остальные таблицы писать `user_id` из этой
мапы вместо `user_name`. Обновить строки `read`/`write` в срезе `tables`.

> Примечание: `-import-sqlite` — легаси-путь для переноса самой первой
> SQLite-базы; сейчас прод уже на Postgres, повторно не используется. Правка —
> чтобы пакет компилировался и тест `import` проходил.

- [ ] **Step 7: Прогнать весь стор**

Run: `go test ./server/internal/store/ -v`
Expected: PASS (все тесты).

- [ ] **Step 8: Прогнать против Postgres**

Run: `make test-pg` (нужен локальный `postgresql@17` + `createdb srpski_test`)
Expected: PASS. Если Postgres локально нет — отметить в коммите как непроверенное, CI/ревьюер прогонит.

- [ ] **Step 9: Коммит**

```bash
git add server/internal/store/
git commit -m "$(printf 'feat(store): key learning state by user_id, add account CRUD\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>')"
```

---

## Task 6: Стор — `identities`

**Files:**
- Create: `server/internal/store/identities.go`, `server/internal/store/identities_test.go`

**Interfaces:**
- Consumes: `auth.NewIdentityID` (Task 1), `Store.db` (`*database`), `Store.CreateUser` (Task 5).
- Produces:
```go
type Identity struct {
	ID, UserID, Provider, ProviderUID string
	Email, PasswordHash               string    // password provider
	EmailVerifiedAt                   string    // RFC3339 or ""
	TgUsername                        string    // telegram provider
	CreatedAt                         string
}

// CreateIdentity inserts a row; caller supplies a generated ID via auth.NewIdentityID().
func (s *Store) CreateIdentity(in Identity) error
// IdentityByProviderUID looks up one identity; sql.ErrNoRows if absent.
func (s *Store) IdentityByProviderUID(provider, uid string) (Identity, error)
// IdentitiesForUser returns every identity of a user.
func (s *Store) IdentitiesForUser(userID string) ([]Identity, error)
// SetEmailVerified stamps email_verified_at = now on a password identity.
func (s *Store) SetEmailVerified(identityID string, now time.Time) error
// SetPasswordHash updates the hash (used by reset / "set password").
func (s *Store) SetPasswordHash(identityID, hash string) error
// AttachPendingTelegram rewrites a "pending:<username>" identity to a real
// tg_id on first Telegram login. Returns false if no pending row matched.
func (s *Store) AttachPendingTelegram(username, tgID string) (bool, error)
```

- [ ] **Step 1: Тесты** — `identities_test.go`, кейсы:
  - `TestCreateAndFindIdentity` — создать `password`-identity, найти по `("password", "grisha@x.io")`, поля совпадают; `IdentityByProviderUID("password","nope")` → `sql.ErrNoRows`.
  - `TestIdentitiesForUser` — у юзера две identity (password + telegram) → len 2.
  - `TestSetEmailVerifiedAndPasswordHash` — после `SetEmailVerified` поле не пустое; после `SetPasswordHash` — новое значение.
  - `TestAttachPendingTelegram` — вставить `provider_uid='pending:llladnooo'`; `AttachPendingTelegram("llladnooo","123456")` → `true`, строка теперь `provider_uid='123456'`; повторный вызов → `false`.
- [ ] **Step 2: Прогнать — падает.** Run: `go test ./server/internal/store/ -run TestIdentit -v` → FAIL.
- [ ] **Step 3: Реализация** `identities.go` — прямые `INSERT`/`SELECT`/`UPDATE` с `?`-плейсхолдерами (как в `store.go`). Образец на `CreateIdentity` + `IdentityByProviderUID`:
```go
const identityCols = `id, user_id, provider, provider_uid,
	COALESCE(email,''), COALESCE(password_hash,''), COALESCE(email_verified_at,''),
	COALESCE(tg_username,''), created_at`

func (s *Store) CreateIdentity(in Identity) error {
	_, err := s.db.Exec(`INSERT INTO identities
		(id, user_id, provider, provider_uid, email, password_hash, email_verified_at, tg_username, created_at)
		VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)`,
		in.ID, in.UserID, in.Provider, in.ProviderUID,
		nullIf(in.Email), nullIf(in.PasswordHash), nullIf(in.EmailVerifiedAt), nullIf(in.TgUsername),
		in.CreatedAt)
	return err
}

func (s *Store) IdentityByProviderUID(provider, uid string) (Identity, error) {
	return scanIdentity(s.db.QueryRow(
		`SELECT `+identityCols+` FROM identities WHERE provider = ? AND provider_uid = ?`, provider, uid))
}
```
Хелперы в `identities.go`: `nullIf(s string) any` (пустая строка → `nil`),
`scanIdentity(row interface{ Scan(...any) error }) (Identity, error)`.
`AttachPendingTelegram`:
```go
func (s *Store) AttachPendingTelegram(username, tgID string) (bool, error) {
	res, err := s.db.Exec(
		`UPDATE identities SET provider_uid = ?, tg_username = ?
		 WHERE provider = 'telegram' AND provider_uid = ?`,
		tgID, username, "pending:"+username)
	if err != nil {
		return false, err
	}
	n, _ := res.RowsAffected()
	return n > 0, nil
}
```
- [ ] **Step 4: Прогнать — проходит.** Run: `go test ./server/internal/store/ -run TestIdentit -v` → PASS.
- [ ] **Step 5: Коммит** — `feat(store): identities CRUD`.

---

## Task 7: Стор — `sessions`

**Files:**
- Create: `server/internal/store/sessions.go`, `server/internal/store/sessions_test.go`

**Interfaces:**
- Consumes: `Store.db`, `Store.CreateUser`.
- Produces:
```go
type Session struct {
	TokenHash, UserID, CreatedAt, LastSeenAt, ExpiresAt, UserAgent string
}

// CreateSession stores a session; tokenHash is hex(sha256(rawToken)) computed by the caller.
func (s *Store) CreateSession(tokenHash, userID, userAgent string, now, expires time.Time) error
// SessionByHash returns a live session; sql.ErrNoRows if missing or expired-at <= now.
func (s *Store) SessionByHash(tokenHash string, now time.Time) (Session, error)
// TouchSession bumps last_seen_at and slides expires_at, but not past the 90-day hard cap from created_at.
func (s *Store) TouchSession(tokenHash string, now time.Time) error
func (s *Store) DeleteSession(tokenHash string) error
func (s *Store) DeleteUserSessions(userID string) error
func (s *Store) DeleteUserSessionsExcept(userID, keepTokenHash string) error
// ListUserSessions returns sessions newest-first for the profile "devices" list.
func (s *Store) ListUserSessions(userID string) ([]Session, error)
// DeleteExpiredSessions is a housekeeping sweep (called opportunistically).
func (s *Store) DeleteExpiredSessions(now time.Time) (int64, error)
```
- Константы в `sessions.go`: `sessionSlide = 30 * 24 * time.Hour`, `sessionMaxAge = 90 * 24 * time.Hour`.

- [ ] **Step 1: Тесты** — `sessions_test.go`:
  - `TestSessionRoundTrip` — create, `SessionByHash` возвращает её; неизвестный хеш → `ErrNoRows`.
  - `TestSessionExpiry` — create с `expires = now.Add(-time.Minute)` → `SessionByHash(now)` → `ErrNoRows`.
  - `TestTouchSessionCap` — create с `created = now-100d` (подставить вручную UPDATE), `TouchSession` → `expires_at` не дальше `created + 90d`.
  - `TestDeleteUserSessionsExcept` — 3 сессии юзера, `DeleteUserSessionsExcept(uid, keep)` → остаётся 1 (keep).
  - `TestDeleteExpiredSessions` — 2 живые + 2 протухшие → sweep вернул 2, живых 2.
- [ ] **Step 2: Прогнать — падает.**
- [ ] **Step 3: Реализация.** `SessionByHash` — `WHERE token_hash = ? AND expires_at > ?`. `TouchSession`:
```go
func (s *Store) TouchSession(tokenHash string, now time.Time) error {
	var createdStr string
	if err := s.db.QueryRow(`SELECT created_at FROM sessions WHERE token_hash = ?`, tokenHash).Scan(&createdStr); err != nil {
		return err
	}
	created, _ := time.Parse(time.RFC3339, createdStr)
	slide := now.Add(sessionSlide)
	if cap := created.Add(sessionMaxAge); slide.After(cap) {
		slide = cap
	}
	_, err := s.db.Exec(`UPDATE sessions SET last_seen_at = ?, expires_at = ? WHERE token_hash = ?`,
		now.UTC().Format(time.RFC3339), slide.UTC().Format(time.RFC3339), tokenHash)
	return err
}
```
- [ ] **Step 4: Прогнать — проходит.**
- [ ] **Step 5: Коммит** — `feat(store): sessions CRUD`.

---

## Task 8: Стор — `email_tokens`

**Files:**
- Create: `server/internal/store/email_tokens.go`, `server/internal/store/email_tokens_test.go`

**Interfaces:**
- Consumes: `Store.db`, `Store.CreateIdentity` (Task 6).
- Produces:
```go
type EmailToken struct {
	TokenHash, IdentityID, Kind, CreatedAt, ExpiresAt, UsedAt string
}

// CreateEmailToken stores a token; tokenHash is hex(sha256(raw)) from the caller. kind is "verify" | "reset".
func (s *Store) CreateEmailToken(tokenHash, identityID, kind string, now, expires time.Time) error
// UseEmailToken atomically marks a live, unused token used and returns its identity_id.
// sql.ErrNoRows if the token is unknown, already used, expired, or the wrong kind.
func (s *Store) UseEmailToken(tokenHash, kind string, now time.Time) (identityID string, err error)
// DeleteIdentityTokens drops every token of a kind for an identity (e.g. on successful reset).
func (s *Store) DeleteIdentityTokens(identityID, kind string) error
```

- [ ] **Step 1: Тесты** — `email_tokens_test.go`:
  - `TestUseEmailTokenHappy` — create `verify` TTL +24h → `UseEmailToken(hash,"verify",now)` вернул `identityID`; повторный вызов → `ErrNoRows` (уже использован).
  - `TestUseEmailTokenExpired` — TTL −1h → `ErrNoRows`.
  - `TestUseEmailTokenWrongKind` — создан `verify`, спросили `reset` → `ErrNoRows`.
  - `TestDeleteIdentityTokens` — 2 `reset` + 1 `verify` → `DeleteIdentityTokens(id,"reset")` → остаётся 1 `verify`.
- [ ] **Step 2: Прогнать — падает.**
- [ ] **Step 3: Реализация.** `UseEmailToken` — одним запросом через `UPDATE ... WHERE token_hash=? AND kind=? AND used_at IS NULL AND expires_at > ? RETURNING identity_id` **не** портируемо (SQLite `RETURNING` есть с 3.35, modernc поддерживает; Postgres тоже). Проще и надёжнее — транзакция: `SELECT identity_id ... FOR UPDATE`-семантика не нужна при одном инстансе:
```go
func (s *Store) UseEmailToken(tokenHash, kind string, now time.Time) (string, error) {
	tx, err := s.db.Begin()
	if err != nil {
		return "", err
	}
	defer tx.Rollback()
	var identityID string
	err = tx.QueryRow(`SELECT identity_id FROM email_tokens
		WHERE token_hash = ? AND kind = ? AND used_at IS NULL AND expires_at > ?`,
		tokenHash, kind, now.UTC().Format(time.RFC3339)).Scan(&identityID)
	if err != nil {
		return "", err // sql.ErrNoRows propagates
	}
	if _, err := tx.Exec(`UPDATE email_tokens SET used_at = ? WHERE token_hash = ?`,
		now.UTC().Format(time.RFC3339), tokenHash); err != nil {
		return "", err
	}
	return identityID, tx.Commit()
}
```
- [ ] **Step 4: Прогнать — проходит.**
- [ ] **Step 5: Коммит** — `feat(store): email_tokens CRUD`.

---

## Финальная проверка плана (часть 1)

- [ ] **Весь пакет стора зелёный:** `go test ./server/internal/store/ ./server/internal/auth/ -v`
- [ ] **Сборка сервера:** `go build ./server/...` — компилируется (API ещё зовёт старые сигнатуры? — на этом этапе `api.go` не трогали, но `store.User(name)` там используется; если `go build ./server/...` падает в `internal/api`, это ожидаемо — часть 2 чинит вызовы. Проверить, что падает **только** `internal/api`, а `store`/`auth` чисты.)
- [ ] **Прогон против Postgres:** `make test-pg` (или отметить как отложенное на ревьюера).

> **Раскатка на прод (rollout-шаги 1–2 из спеки) — вручную, после мержа:**
> 1. Деплой с раннером и `001`/`002`/`003`. Перед деплоем — **ручной дамп PG**
>    (бэкап Dokploy или `pg_dump`), прогон миграций на копии дампа локально.
> 2. После деплоя: `schema_migrations` содержит версии 1–3; `Гриша`/`Алина`
>    на месте с прогрессом (`SELECT name FROM users`), мусора нет,
>    `/api/health` зелёный. Приложение ещё ходит по `X-User` — это чинит
>    часть 2, но состояние читается по `user_id`, значит `handlers.user()`
>    временно сломан → **часть 2 деплоится следом без разрыва только если
>    части 1 и 2 мержатся и катятся вместе**. См. примечание ниже.

> ⚠️ **Важно про порядок раскатки.** Часть 1 меняет ключ состояния, из-за чего
> старый код `api.handlers.user()` (ищет `s.User(имя)`) перестаёт находить
> прогресс. Поэтому **части 1 и 2 деплоятся одним релизом** (одна ветка
> `feature/auth-postgres-profile`, один Redeploy). Части разбиты только для
> удобства ревью и исполнения, не для раздельной раскатки.

---
